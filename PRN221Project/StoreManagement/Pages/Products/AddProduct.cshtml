@page
@using StoreManagement.DataAcces;
@model StoreManagement.Pages.Products.AddProductModel
@{
}

<div class="container">
    <h1>Add a Product</h1>
    <form method="post">
        <div class="form-group">
            <label>Product Name</label>
            <input type="text" class="form-control" asp-for="AddProductView.ProductName" name="productName" placeholder="Enter product name">
        </div>
        <div class="form-group">
            <label>Category</label>
            <select class="form-select mb-4 mt-2" asp-for="AddProductView.CategoryId" name="categoryId" aria-label="Category">
                <option value="0">--Not selected--</option>
                @foreach (Category c in Model.Categories)
                {
                    <option value="@c.CategoryId">@c.CategoryName</option>
                }
            </select>
        </div>
        <div class="form-group">
            <label>Supplier</label>
            <select class="form-select mb-4 mt-2" asp-for="AddProductView.SupplierId" name="supplierId" aria-label="Category">
                <option value="0">--Not selected--</option>
                @foreach (Supplier s in Model.Suppliers)
                {
                    <option value="@s.SupplierId">@s.CompanyName</option>
                }
            </select>
        </div>
        <div class="form-group">
            <label>Price</label>
            <input type="number" asp-for="AddProductView.UnitPrice" name="unitPrice" />
        </div>
        <div class="form-group">
            <label>Units In Stock</label>
            <input type="numer" asp-for="AddProductView.UnitsInStock" name="unitsInStock" />
        </div>
        <button type="submit" class="btn btn-primary">Save</button>
    </form>
</div>